Alembic migrations for the CRC Assessment Platform.

Single-database, synchronous configuration. The database URL is supplied at
runtime from application settings, not from alembic.ini, so credentials are
never committed.

Common commands (run from the backend directory):

    alembic revision --autogenerate -m "description"
    alembic upgrade head
    alembic downgrade -1
    alembic current